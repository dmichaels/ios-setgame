#!/bin/bash
HOST=https://api.logicard.dmichaels.dev
METHOD=GET
# APIKEY=$(cat .key)
APIKEY=.0turangalila
URL="$HOST/$*"
echo $METHOD $URL
curl -s \
  -H "X-API-Key: $APIKEY" \
  -H "Content-Type: application/json" \
  -X $METHOD "$URL" \
   | python -m json.tool
